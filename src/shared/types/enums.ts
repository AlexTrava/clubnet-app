export enum RoutersPaths {
  MAIN = '/',
  ADMIN = '/admin',
  PROFILE = '/profile',
  USERDETAIL = 'profile/:id',
  LOGIN = '/login',
  NOFOUND = '*'
}
export enum Status {
  LOADING = 'loading',
  SUCCESS = 'success',
  ERROR = 'error'
}
export enum MessagesType {
  NOTIFICATION = 'notification',
  MESSAGES = 'messages'
}
